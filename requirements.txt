# CIEML 2.0 — full pipeline dependencies (Phases 1–8)
# Install: python -m pip install -r requirements.txt

numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
PyYAML>=6.0
openpyxl>=3.1
statsmodels>=0.14
tqdm>=4.66

# Parquet read/write (Stages 0, 2, 4 and phase loaders)
pyarrow>=14.0

# Stage 6 — optional HDBSCAN path in regime discovery
hdbscan>=0.8.33

# Stage 8 — explainable ML / XAI
shap>=0.44
xgboost>=2.0
lightgbm>=4.0

# Stage 10 — Open-Meteo archive fetch (needs network unless offline cache is used)
requests>=2.31
